version: "1"
checks:
  misconfig:
    # Scan for misconfigurations in Docker and YAML files
    enabled: true
  vuln:
    # Scan for vulnerabilities in images
    enabled: true
  secret:
    # Scan for secrets in code
    enabled: true
severity:
  - HIGH
  - CRITICAL
skip-dirs:
  - tests/
